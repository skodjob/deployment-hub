apiVersion: integreatly.org/v1alpha1
kind: GrafanaDashboard
metadata:
  labels:
    app: debezium
    monitoring-key: middleware
  name: debezium
spec:
  configMapRef:
    name: grafana-dashboard-debezium-config
    key: debezium
  plugins:
    - name: snuids-trafficlights-panel
      version: 1.4.6
