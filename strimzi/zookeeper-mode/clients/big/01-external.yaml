---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: big-external-producer
  name: big-external-producer
spec:
  replicas: 3
  selector:
    matchLabels:
      app: big-external-producer
  template:
    metadata:
      labels:
        app: big-external-producer
    spec:
      serviceAccountName: clients-sa
      containers:
        - name: big-external-producer
          image: quay.io/strimzi-test-clients/test-clients:0.5.1-kafka-3.4.0
          imagePullPolicy: Always
          resources:
            requests:
              memory: 50Mi
              cpu: 50m
            limits:
              memory: 350Mi
              cpu: 250m
          env:
            - name: CA_CRT
              value: ${secrets:strimzi-kafka/anubis-cluster-ca-cert:ca.crt}
            - name: USER_CRT
              value: ${secrets:strimzi-kafka/kafka-anubis-external-producer:user.crt}
            - name: USER_KEY
              value: ${secrets:strimzi-kafka/kafka-anubis-external-producer:user.key}
            - name: BOOTSTRAP_SERVERS
              value: anubis-kafka-automation-bootstrap-strimzi-kafka.apps.worker-01.strimzi.app-services-dev.net:443
            - name: TOPIC
              value: big-anubis-external-data
            - name: DELAY_MS
              value: "100"
            - name: MESSAGE
              value: |
                I am sending a little big bigger messages, no random ones, but a longer than hello world, wdyt?
                I started to write a story about it, but it seems the data are not enough to fill all the pages that book requires.
                Another line! What a message, right? I think it is enough!
            - name: LOG_LEVEL
              value: "INFO"
            - name: MESSAGE_COUNT
              value: "10000000"
            - name: PRODUCER_ACKS
              value: all
            - name: ADDITIONAL_CONFIG
            - name: CLIENT_TYPE
              value: KafkaProducer
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: nodetype
                    operator: In
                    values:
                      - infra
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: big-external-streams
  name: big-external-streams
spec:
  replicas: 3
  selector:
    matchLabels:
      app: big-external-streams
  template:
    metadata:
      labels:
        app: big-external-streams
    spec:
      serviceAccountName: clients-sa
      containers:
        - name: big-external-streams
          image: quay.io/strimzi-test-clients/test-client-kafka-streams@sha256:349a368470d4510964ccb44a18bc2d57016dba3b642f963933dccf95a8eed185
          imagePullPolicy: Always
          resources:
            requests:
              memory: 50Mi
              cpu: 50m
            limits:
              memory: 250Mi
              cpu: 250m
          env:
            - name: CA_CRT
              value: ${secrets:strimzi-kafka/anubis-cluster-ca-cert:ca.crt}
            - name: USER_CRT
              value: ${secrets:strimzi-kafka/kafka-anubis-external-streams:user.crt}
            - name: USER_KEY
              value: ${secrets:strimzi-kafka/kafka-anubis-external-streams:user.key}
            - name: BOOTSTRAP_SERVERS
              value: anubis-kafka-automation-bootstrap-strimzi-kafka.apps.worker-01.strimzi.app-services-dev.net:443
            - name: APPLICATION_ID
              value: big-anubis-external-data-streams
            - name: SOURCE_TOPIC
              value: big-anubis-external-data
            - name: TARGET_TOPIC
              value: big-anubis-external-data-reversed
            - name: LOG_LEVEL
              value: "INFO"
            - name: CLIENT_TYPE
              value: KafkaStreams
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: nodetype
                    operator: In
                    values:
                      - infra
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: big-external-consumer
  name: big-external-consumer
spec:
  replicas: 4
  selector:
    matchLabels:
      app: big-external-consumer
  template:
    metadata:
      labels:
        app: big-external-consumer
    spec:
      serviceAccountName: clients-sa
      containers:
        - name: big-external-consumer
          image: quay.io/strimzi-test-clients/test-clients:0.5.1-kafka-3.4.0
          imagePullPolicy: Always
          resources:
            requests:
              memory: 50Mi
              cpu: 50m
            limits:
              memory: 250Mi
              cpu: 250m
          env:
            - name: CA_CRT
              value: ${secrets:strimzi-kafka/anubis-cluster-ca-cert:ca.crt}
            - name: USER_CRT
              value: ${secrets:strimzi-kafka/kafka-anubis-external-consumer:user.crt}
            - name: USER_KEY
              value: ${secrets:strimzi-kafka/kafka-anubis-external-consumer:user.key}
            - name: BOOTSTRAP_SERVERS
              value: anubis-kafka-automation-bootstrap-strimzi-kafka.apps.worker-01.strimzi.app-services-dev.net:443
            - name: TOPIC
              value: big-anubis-external-data-reversed
            - name: GROUP_ID
              value: big-anubis-external-data-consumer
            - name: LOG_LEVEL
              value: "INFO"
            - name: MESSAGE_COUNT
              value: "10000000"
            - name: CLIENT_TYPE
              value: KafkaConsumer
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: nodetype
                    operator: In
                    values:
                      - infra
