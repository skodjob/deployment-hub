---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: kafka-external-producer
  name: kafka-external-producer
  namespace: strimzi-clients
spec:
  replicas: 15
  selector:
    matchLabels:
      app: kafka-external-producer
  template:
    metadata:
      labels:
        app: kafka-external-producer
    spec:
      containers:
        - name: kafka-external-producer
          image: quay.io/strimzi-test-clients/test-client-kafka-producer@sha256:3d3213c47008c780857d9a79d94309a9755aeda80a281d7f6f1f8839a6cf9014
          imagePullPolicy: Always
          resources:
            requests:
              memory: 50Mi
              cpu: 50m
            limits:
              memory: 250Mi
              cpu: 250m
          env:
            - name: CA_CRT
              value: ${secrets:strimzi-kafka/anubis-cluster-ca-cert:ca.crt}
            - name: USER_CRT
              value: ${secrets:strimzi-kafka/kafka-external-producer:user.crt}
            - name: USER_KEY
              value: ${secrets:strimzi-kafka/kafka-external-producer:user.key}
            - name: BOOTSTRAP_SERVERS
              value: anubis-kafka-automation-bootstrap-strimzi-kafka.apps.worker-01.strimzi.app-services-dev.net:443
            - name: TOPIC
              value: kafka-anubis-external-data
            - name: DELAY_MS
              value: "3"
            - name: LOG_LEVEL
              value: "INFO"
            - name: MESSAGE_COUNT
              value: "10000000"
            - name: PRODUCER_ACKS
              value: all
            - name: ADDITIONAL_CONFIG
            - name: BLOCKING_PRODUCER
              value: "true"
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: nodetype
                    operator: In
                    values:
                      - infra
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: kafka-external-streams
  name: kafka-external-streams
  namespace: strimzi-clients
spec:
  replicas: 10
  selector:
    matchLabels:
      app: kafka-external-streams
  template:
    metadata:
      labels:
        app: kafka-external-streams
    spec:
      containers:
        - name: kafka-external-streams
          image: quay.io/strimzi-test-clients/test-client-kafka-streams@sha256:349a368470d4510964ccb44a18bc2d57016dba3b642f963933dccf95a8eed185
          imagePullPolicy: Always
          resources:
            requests:
              memory: 50Mi
              cpu: 50m
            limits:
              memory: 250Mi
              cpu: 250m
          env:
            - name: CA_CRT
              value: ${secrets:strimzi-kafka/anubis-cluster-ca-cert:ca.crt}
            - name: USER_CRT
              value: ${secrets:strimzi-kafka/kafka-external-streams:user.crt}
            - name: USER_KEY
              value: ${secrets:strimzi-kafka/kafka-external-streams:user.key}
            - name: BOOTSTRAP_SERVERS
              value: anubis-kafka-automation-bootstrap-strimzi-kafka.apps.worker-01.strimzi.app-services-dev.net:443
            - name: APPLICATION_ID
              value: kafka-anubis-external-data-streams
            - name: SOURCE_TOPIC
              value: kafka-anubis-external-data
            - name: TARGET_TOPIC
              value: kafka-anubis-external-data-reversed
            - name: LOG_LEVEL
              value: "INFO"
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: nodetype
                    operator: In
                    values:
                      - infra
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: kafka-external-consumer
  name: kafka-external-consumer
  namespace: strimzi-clients
spec:
  replicas: 20
  selector:
    matchLabels:
      app: kafka-external-consumer
  template:
    metadata:
      labels:
        app: kafka-external-consumer
    spec:
      containers:
        - name: kafka-external-consumer
          image: quay.io/strimzi-test-clients/test-client-kafka-consumer@sha256:0e92ed607ed9caca575528690ae82741def274f3fbee4680991d35c9f02b554e
          imagePullPolicy: Always
          resources:
            requests:
              memory: 50Mi
              cpu: 50m
            limits:
              memory: 250Mi
              cpu: 250m
          env:
            - name: CA_CRT
              value: ${secrets:strimzi-kafka/anubis-cluster-ca-cert:ca.crt}
            - name: USER_CRT
              value: ${secrets:strimzi-kafka/kafka-external-consumer:user.crt}
            - name: USER_KEY
              value: ${secrets:strimzi-kafka/kafka-external-consumer:user.key}
            - name: BOOTSTRAP_SERVERS
              value: anubis-kafka-automation-bootstrap-strimzi-kafka.apps.worker-01.strimzi.app-services-dev.net:443
            - name: TOPIC
              value: kafka-anubis-external-data-reversed
            - name: GROUP_ID
              value: kafka-anubis-external-data-consumer
            - name: LOG_LEVEL
              value: "INFO"
            - name: MESSAGE_COUNT
              value: "10000000"
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: nodetype
                    operator: In
                    values:
                      - infra
